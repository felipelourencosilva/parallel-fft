#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1e3;
int result[MAXN];

signed main() {
    ios::sync_with_stdio(0);cin.tie(0);

    memset(result, 0, sizeof(result));

    for(int i = 0; i < MAXN; i++) {
        cout << result[i] << " ";
    }
    
    cout << "\n";
    return 0;
}